{
  "name": "pwl7-chatbot-backend",
  "version": "0.1.0",
  "description": "Backend for the pwl7 parenting-help chatbot: login, per-family memory, and an OpenAI-backed chat endpoint (falls back to a scripted demo when no API key is set).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.3.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "openai": "^4.55.4"
  }
}
